package com.lrosas.examen2do.ui.theme.navegation

/**
 * Rutas de navegación empleadas por Navigation-Compose.
 * Cada valor del enum coincide con el nombre de la pantalla
 * que registra el NavHost.
 */
enum class NavRoutes {
    HOME,
    FORM,
    QUIZ,
    RESULT
}